# AWS Amplify Configuration
# These values are automatically generated when you run `npm run amplify:sandbox`
# You don't need to set these manually - they're provided by amplify_outputs.json

# Optional: Custom environment variables for your app
# VITE_APP_NAME=My Awesome App
# VITE_API_URL=https://api.example.com

# Development only - not needed for production
# NODE_ENV=development
